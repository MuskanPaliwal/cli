I want to discuss and implement split Part 3 (GMT-003): session Git metadata migration in entireio/cli.

Review and assess the task independently before editing. You own that review; this handoff supplies starting context, not a substitute for inspecting the current implementation. Decide whether the task is still needed, already solved, stale, over-scoped, or better handled differently. Report your findings and recommendation first, then proceed with the scoped implementation if the review supports it. Do not stop merely to request implementation approval; this task is authorized.

Worktree and starting state:
- Work in /Users/muskanpaliwal/Work/open-source/entire/cli/refactor-session-metadata-resolution on branch refactor-session-metadata-resolution.
- This worktree already exists. At handoff preparation it was clean, at ee6d53f3d3398267358e8f9d306267c87a088101, with no commits beyond that original planning baseline. Recheck before changing it; preserve any intervening work.
- Part 1, canonical metadata owner, merged as PR https://github.com/entireio/cli/pull/2241 (merge commit 63a0232c355632d35b4bac5692385d3294d02506).
- Part 2, checkpoint migration, merged as PR https://github.com/entireio/cli/pull/2254 (merge commit b14d9a92b90a1da453b9f1484b3a82755b27ad9d).
- Refresh upstream/main and fast-forward this branch onto it if the clean/no-unique-commits condition still holds. Current main was 61dac01ed02a40a3537f5896021d3db288b32ced when checked on 2026-09-05; recheck live state. Use merged main, rather than cherry-picking old prerequisite branches or the original combined implementation. Do not reset away changes.
- Read local AGENTS.md and CONTRIBUTING.md. The split plan also calls for refreshing the shared bare-layout root .codex/hooks.json from the active worktree before its agent session; inspect the existing mechanism and preserve other active sessions' configuration.

Planning references:
- The local, untracked split plan is /Users/muskanpaliwal/Work/open-source/entire/cli/refactor-git-metadata-traversal/plan.md, titled “Plan: Split Git Metadata Traversal Consolidation.” Read GMT-003 plus dependencies, failure policies, and validation requirements. It is in the original worktree, not the Part 3 worktree.
- Its top-level “implementation has not started” status is stale. Both prerequisite PRs are merged.
- The same original worktree contains GIT_METADATA_TRAVERSAL_IMPLEMENTATION_PLAN.md and GIT_METADATA_TRAVERSAL_THERMO_NUCLEAR_REVIEW.md. These and original PR https://github.com/entireio/cli/pull/2190 provide historical design/test references; the split plan defines this slice. Do not mechanically port the monolithic patch or treat historical validation as proof for this branch.

Independent review:
- Inspect current code, callers, tests, recent history, prerequisite PRs, and relevant CI state. Use existing repository discovery/history tools where available and search for reusable utilities before adding Go code.
- Confirm the canonical contract of gitrepo.ResolveWorktreeMetadata and WorktreeMetadata{GitDir, CommonDir, WorktreeID}. Physical metadata traversal belongs there. The resolver stays explicit-root, context-free, option-free, cache-free, subprocess-free, and all-or-error; it returns lexical absolute paths and uses physical identity only at boundaries that require it.
- Trace all callers of APIs being removed so this intermediate slice remains buildable without absorbing later slices. Flag a real dependency conflict rather than silently expanding scope.

Implementation scope:
- Migrate session state construction, adoption, and sweep to canonical metadata resolution.
- Remove session forwarding APIs and gitdir's duplicate common-directory subprocess/cache machinery, including CommonDir, CommonDirForWorktree, ClearCache, and cache-only tests where applicable after checking current callers.
- Preserve gitdir.OpenForCurrentWorktree, OpenAt, OpenPathIn, and Reset: they own confined access and shared os.Root lifetime. Do not confuse rooted-handle lifetime with the obsolete metadata cache.
- Preserve empty explicit-root rejection, required session-store failures, rooted/no-follow storage and flock behavior, test isolation, adoption validation, and sweep warning/fallback semantics.
- Retain adoption's combined Git query when it validates an arbitrary user-supplied repository. Distinguish semantic repository validation from redundant traversal of already-known metadata.
- Expected areas are gitdir/gitdir.go, session state, session_adopt, session_sweep, and focused tests. Keep any additional caller edits minimal and justified.

Boundaries:
- Multi-store session-lock physical identity, alias deduplication, ordering, and strategy/session_state.go belong to Part 4. Do not implement that slice here.
- Do not migrate unrelated strategy/hooks, settings, status, Codex, trail, or other Part 5 consumers. Do not delete paths.GetWorktreeID or finalize the ownership guard's legacy ledger here.
- Preserve the current migration-aware ownership guard; the split plan assigns final cleanup to Part 5. Do not restore exceptions already removed by landed work or weaken the guard to accommodate new traversal.
- No new metadata cache, permanent compatibility wrappers, generic Git facade, layout taxonomy, resolver options, product-policy changes, or checkpoint persistence redesign.

Validation and output:
- Add focused behavioral coverage for ordinary and linked worktrees, malformed/empty roots, metadata repair without cache clearing, rooted session storage, adoption validation, and sweep failure policy as relevant to the actual changes.
- Prove session construction removes duplicate common-directory Git traversal without increasing whole-operation subprocess counts. Separate existing CWD worktree discovery from metadata traversal; use real Trace2 observations and a positive control where appropriate.
- Use isolated temp Git repositories and testutil helpers. Tests/subtests use t.Parallel unless they modify process-global state. Preserve config/cache/keyring and spawned-binary isolation.
- Run focused tests for affected packages, mise run fmt followed by mise run lint, mise run dup (inspect findings), and git diff --check. Run mise run check before any commit; no paid real-agent E2E tests. Report exact commands, outcomes, and any remaining limitations.
- Deliver a scoped patch with a concise account of removed ownership/cache machinery, preserved behavior, tests, and any justified deviations from GMT-003. Compare against refreshed main so Parts 1 and 2 are not presented as new changes.
- Do not push, create or merge PRs, close issues/PRs, label, or post public comments unless explicitly instructed later.
 let's implement this split part 3. i need a proper plan first before you start implementation. one thing or change at a time. change one thing and explain and then move on. run-playbook

---

no we will not delete anything unless eevrything is good to go. i think on my metadata-traversal pr soph's last comment also says the same